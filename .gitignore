# This .gitignore file should be placed at the root of your Unity project directory
#
# Get latest from https://github.com/github/gitignore/blob/main/Unity.gitignore
#
.utmp/
/[Ll]ibrary/
/[Tt]emp/
/[Oo]bj/
/[Bb]uild/
/[Bb]uilds/
/[Ll]ogs/
/[Uu]ser[Ss]ettings/
*.log

# MemoryCaptures can get excessive in size.
# They also could contain extremely sensitive data
/[Mm]emoryCaptures/

# Recordings can get excessive in size
/[Rr]ecordings/

# Uncomment this line if you wish to ignore the asset store tools plugin
# /[Aa]ssets/AssetStoreTools*

# Autogenerated Jetbrains Rider plugin
/[Aa]ssets/Plugins/Editor/JetBrains*

# Visual Studio cache directory
.vs/

# Gradle cache directory
.gradle/

# Autogenerated VS/MD/Consulo solution and project files
ExportedObj/
.consulo/
*.csproj
*.unityproj
*.sln
*.suo
*.tmp
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db

# Unity3D generated meta files
*.pidb.meta
*.pdb.meta
*.mdb.meta

# Unity3D generated file on crash reports
sysinfo.txt

# Builds
*.apk
*.aab
*.unitypackage
*.unitypackage.meta
*.app

# 🆕（2026-07-26）本機建置輸出：可由專案重新產生的大型二進位，不進版控。
# 目前 Builds/Development/ 為 Profiler 驗收用的 Development Build（~173MB）。
# ⚠️ 對照：驗收「截圖」是另一回事——它是 dev-spec §7.4／README 的**真實佐證**，
#    必須進版控，存放於 docs/images/profiler/（該路徑刻意不在此排除）。
/[Bb]uilds/

# Crashlytics generated file
crashlytics-build.properties

# Packed Addressables
/[Aa]ssets/[Aa]ddressable[Aa]ssets[Dd]ata/*/*.bin*

# Temporary auto-generated Android Assets
/[Aa]ssets/[Ss]treamingAssets/aa.meta
/[Aa]ssets/[Ss]treamingAssets/aa/*

# Technical Mentor 個人複習題庫（不上 GitHub）
/_MentorQuiz/

# =========================================================================
# 第三方資產排除（不上 GitHub）
#   法律：Unity Asset Store EULA 禁「二次散佈」付費資產 → 公開 repo 等同免費散佈他人付費產品。
#   工程：大型二進位（FBX/貼圖/音效）污染 .git 歷史、拖慢 clone/pull；核心程式碼與重型美術資產分離維護。
# ⚠️ 這些檔案「已被 git 追蹤」——**光加本 .gitignore 不會取消追蹤**，需再跑 `git rm -r --cached <path>`
#    後 commit 才真正生效（見 WORKLOG「交辦：第三方資產屏蔽 SOP」）。取消追蹤後本機檔案保留、Unity 照常運作。
# ⚠️ 排除後 fresh clone **無法直接編譯/執行**：需持有合法授權者各自重新匯入（Animancer＝執行期核心依賴；
#    Kubold＝Bake_*.asset 依 GUID 引用其子 clip）。建議 repo 內加 README 註明「必要資產與匯入方式」。
# =========================================================================

# Animancer Pro（付費，執行期動畫系統核心依賴）
/[Pp]ackages/com.kybernetik.animancer/

# Kubold Movement Animset Pro（付費 locomotion 資產）
/[Aa]ssets/MovementAnimsetPro/
/[Aa]ssets/MovementAnimsetPro.meta

# Unity StarterAssets（免費但第三方；純大小/分離考量，法律風險低——確認專案不依賴後再取消追蹤）
/[Aa]ssets/StarterAssets/
/[Aa]ssets/StarterAssets.meta
